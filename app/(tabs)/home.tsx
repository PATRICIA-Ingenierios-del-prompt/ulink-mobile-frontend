// Home is now a tab screen inside (tabs)/.
// The actual implementation lives in app/home.tsx and is re-exported here
// so it participates in the tab navigator (enabling seamless nav bar persistence).
export { default } from "../home";
